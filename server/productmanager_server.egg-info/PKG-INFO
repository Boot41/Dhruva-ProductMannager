Metadata-Version: 2.4
Name: productmanager-server
Version: 0.1.0
Summary: FastAPI backend for ProductManager
Author: Your Name
License: MIT
Requires-Python: >=3.9
Description-Content-Type: text/markdown
Requires-Dist: fastapi>=0.115.0
Requires-Dist: uvicorn[standard]>=0.30.0
Requires-Dist: pydantic>=2.7.0
Requires-Dist: pydantic-settings>=2.3.0
Requires-Dist: langchain>=0.2.12
Requires-Dist: langchain-openai>=0.1.22
Requires-Dist: langchain-community>=0.2.11
Requires-Dist: langchain-google-genai>=0.1.0
Requires-Dist: tiktoken>=0.7.0

# ProductManager Server (FastAPI + uv)

## Quick start

1. Copy the example env and add your secrets
   ```bash
   cp .env.example .env
   # edit .env to add your keys
   ```

2. Install dependencies with uv
   ```bash
   uv sync
   ```

3. Run the API (reload for dev)
   ```bash
   uv run uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
   ```

Open http://127.0.0.1:8000/docs for interactive docs.

## Project layout

- `app/main.py` — FastAPI application entrypoint.
- `app/core/config.py` — Settings loaded from `.env` using pydantic-settings.
- `.env` — Local secrets (ignored by git). See `.env.example` for keys.
- `pyproject.toml` — uv-managed project metadata and dependencies.

## Environment

We load configuration from `.env` at project root. Example:

```
API_KEY=replace-with-your-key
ANOTHER_SERVICE_TOKEN=...
```

Values are available via `Settings` in `app/core/config.py`.
